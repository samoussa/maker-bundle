The <info>%command.name%</info> command generates a new service class.

<info>php %command.full_name% Coolservice</info>

If the argument is missing, the command will ask for the service class name interactively.
